# Development: leave empty — Vite proxy handles /api → localhost:8000
VITE_API_URL=

# Production (Vercel): set to your deployed backend URL
# VITE_API_URL=https://your-campusiq-backend.onrender.com/api
